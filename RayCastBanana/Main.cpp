#include "MapParser.h"
#include "Model.h"
#include "view.h"

int main()
{
    int resolutionX = 640;
    int resolutionY = 480;

    Model model(resolutionX, resolutionY);
    View view(resolutionX, resolutionY);
}
